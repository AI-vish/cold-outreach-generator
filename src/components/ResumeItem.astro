---
import { Image } from "astro:assets";
import type { ResumeItem } from "../types/config";

type Props = ResumeItem;

const { title, company, date } = Astro.props;
---

<a
  href={company.url}
  class="py-3 px-4 -mx-4 rounded-xl flex items-center justify-between group-hover:opacity-50 hover:!opacity-100 hover:bg-white/[0.03] transition-all"
>
  <div class="flex items-center gap-4">
    <Image
      src={company.image}
      alt={company.name}
      width={40}
      height={40}
      class="rounded-full w-[40px] h-[40px] object-cover"
    />
    <div>
      <h3 class="font-medium">{title}</h3>
      <p class="text-sm opacity-60">{company.name}</p>
    </div>
  </div>
  <p class="text-sm opacity-60">{date}</p>
</a>
